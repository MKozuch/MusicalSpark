#pragma once

void playKurkiTrzy(){
  using namespace Note;
  playNote(C, 500);
  playNote(C, 500);
  playNote(G, 500);
  playNote(G, 500);
  playNote(A, 500);
  playNote(A, 500);
  playNote(G, 1000);

  playNote(F, 500);
  playNote(F, 500);
  playNote(E, 500);
  playNote(E, 500);
  playNote(D, 500);
  playNote(D, 500);
  playNote(C, 1000);

  playNote(G, 500);
  playNote(G, 500);
  playNote(F, 500);
  playNote(F, 500);
  playNote(E, 500);
  playNote(E, 500);
  playNote(D, 1000);

  playNote(G, 500);
  playNote(G, 500);
  playNote(F, 500);
  playNote(F, 500);
  playNote(E, 500);
  playNote(E, 500);
  playNote(D, 1000);


  playNote(C, 500);
  playNote(C, 500);
  playNote(G, 500);
  playNote(G, 500);
  playNote(A, 500);
  playNote(A, 500);
  playNote(G, 1000);

  playNote(F, 500);
  playNote(F, 500);
  playNote(E, 500);
  playNote(E, 500);
  playNote(D, 500);
  playNote(D, 500); 
  playNote(C, 1000);
  playNoNote(1000);
}